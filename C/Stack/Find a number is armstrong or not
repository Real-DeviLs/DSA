#include <stdio.h>
int main()
{
    int n,sum=0,b,rem;
    printf("Enter the number- ");
    scanf("%d",&n);
    b=n;
    while(n>0)
    {
      rem=n%10;
      sum=sum+(rem*rem*rem);
      n=n/10;
    }
if(b==sum)
printf("armstrong");
else
printf("Not armstrog");
 return 0;
}
